#include <bits/stdc++.h>

using namespace std;

int tcNum, n;

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  while (cin >> n && n) cout << "Case " << ++tcNum << ": " << n / 2 << '\n';
}
